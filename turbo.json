{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".output/**"]
    },
    "lint": {
      "dependsOn": ["format", "^lint"],
      "cache": false
    },
    "format": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
